import {Socket} from 'socket.io-client';

interface IProps {
  socket: Socket //  Socket<DefaultEventsMap, DefaultEventsMap>
}

export const GameStarted = ({ socket } : IProps) => {



  return (
    <>

    </>
  );
};